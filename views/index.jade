.articles
  h3 recent articles 
  each article in articles.slice(0, 3)
    .article
      h4
        a(href=article.name + '/')= article.title
        date = article.date
        span(title=strftime('%B %d, %Y %H:%M UTC', date))= strftime('%Y.%m.%d', date)
      | !{article.desc}
a(id='morelink', href='articles') older...
